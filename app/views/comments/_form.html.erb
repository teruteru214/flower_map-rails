<label for="my-modal-3" class="btn btn-primary"><%= t 'defaults.comment' %></label>
<input type="checkbox" id="my-modal-3" class="modal-toggle" />
<div class="modal">
  <div class="modal-box relative">
    <label for="my-modal-3" class="btn btn-sm btn-circle btn-outline absolute right-2 top-2">✕</label>
      <%= form_with model: @comment, url: [@flower, @comment], local: true, class: 'flex' do |f| %>
        <%= f.text_area :body, class: 'textarea textarea-bordered w-96', placeholder: (t 'defaults.placeholder_comment') %>
        <%= f.submit (t 'defaults.post_comment'), class: 'btn btn-sm mt-11 ml-1' %>
      <% end %>
    </div>
  </div>
</div>
