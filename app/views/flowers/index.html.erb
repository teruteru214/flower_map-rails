<div class="bg-base-200">
  <div class="text-center">
    <h1 class="text-3xl text-center font-bold p-10"><%= t '.title' %></h1>
    <% if @flowers.present? %>
      <%= render @flowers %>
    <% else %>
      <p><%= t '.not_posted' %></p>
    <% end %>
    <div class="flex justify-center">
      <%= paginate @flowers, theme: 'flower' %>
    </div>
  </div>
</div>
