<div class="hero min-h-screen bg-base-200">
  <div class="hero-content text-center">
    <div class="max-w-md">
      <h1 class="text-3xl font-bold">投稿された花</h1>
      <% if @flowers.present? %>
        <%= render @flowers %>
      <% else %>
        <p><%= '投稿なし' %></p>
      <% end %>
    </div>
  </div>
</div>
